# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        disabled: true
    username:
        label: '@username'
        disabled: true
